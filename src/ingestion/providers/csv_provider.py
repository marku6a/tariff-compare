"""Adapter placeholder for a recognised CSV meter-reading export."""

# A provider adapter identifies and reads one source format while preserving
# its original fields as strings. It does not convert the export into the
# canonical analytical meter-reading schema; dbt performs that interpretation.
