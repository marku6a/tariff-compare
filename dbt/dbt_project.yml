name: tariff_compare
version: "1.0.0"
config-version: 2
profile: tariff-compare

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  tariff-compare:
    staging:
      +materialized: view
    validation:
      +materialized: table
    intermediate:
      +materialized: table
    marts:
      +materialized: table
